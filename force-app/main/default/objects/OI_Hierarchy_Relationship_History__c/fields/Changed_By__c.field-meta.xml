<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Changed_By__c</fullName>
    <label>Changed By</label>
    <type>Lookup</type>
    <referenceTo>User</referenceTo>
    <relationshipName>OI_Hierarchy_Changes</relationshipName>
    <relationshipLabel>OI Hierarchy Changes</relationshipLabel>
    <required>false</required>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>FR-017. Not marked required — Salesforce only allows SetNull (never Restrict/Cascade) for a Lookup to User, which is incompatible with required=true. OI_HierarchyRelationshipService always populates this via UserInfo.getUserId() at write time regardless.</description>
</CustomField>
