<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Status__c</fullName>
    <label>Status</label>
    <type>Text</type>
    <length>40</length>
    <required>true</required>
    <description>Mirrors OI_Scan_Run__c.Status__c's values (Text, not Picklist, to avoid a second picklist needing to stay in sync with the sObject's).</description>
</CustomField>
